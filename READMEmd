# habitat-compase

## Debugging

```bash
 HAB_DOCKER_OPTS="-p 9229:9229" hab studio enter -D

build habitat-compose; HAB_ORIGIN=v2prop build v2-core-refresh/habitat-compose; hab svc load --force v2prop/core

hab pkg exec core/node node --inspect-brk=0.0.0.0 habitat-compose/habitat-compose.js /hab/svc/core/config
```